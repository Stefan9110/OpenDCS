# Base URL of the current OpenDC backend REST API.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# Optional deploy-under-subpath base path (no leading slash).
# NEXT_BASE_PATH=

# Session the preview build serves until Auth0 is wired up: sample (default) or anonymous.
# NEXT_PUBLIC_AUTH_MODE=sample
